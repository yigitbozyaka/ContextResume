---
"context-resume": minor
---

Add `ctxr pause`, `ctxr resume`, and `ctxr list`: local snapshot store under `~/.context-resume`, git sensor, heuristic summary, "while you were away" base-branch comparison, and secret scrubbing before snapshots are written.
