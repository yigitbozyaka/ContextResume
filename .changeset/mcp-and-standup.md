---
"context-resume": minor
---

Add `ctxr mcp`, a stdio MCP server exposing `get_context`, `list_snapshots`, and `save_snapshot` to Claude Code, Claude Desktop, and any MCP client, and `ctxr standup [--since 24h] [--format markdown]` summarizing recent activity across every repository and branch.
