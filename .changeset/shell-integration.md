---
"context-resume": minor
---

Add `ctxr init` shell hooks (bash, zsh, PowerShell) that log every command's exit code and automatically pause/resume on branch change, `ctxr run` for output capture, test-output parsing for Jest, Vitest, pytest, go test and cargo test, and a rerun action on the resume card.
